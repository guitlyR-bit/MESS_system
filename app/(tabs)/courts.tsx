import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function CourtsScreen() {
  return (
    <ScreenPlaceholder
      title="Kurty"
      description="Prohlížení a rezervace tenisových kurtů. Seznam kurtů bude napojen na Supabase."
    />
  );
}
